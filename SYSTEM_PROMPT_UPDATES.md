# 📝 تحديثات System Prompt - دعم الرياضيات وأشكال الحروف

## التاريخ: 2026-01-11

### ✅ التحديثات المطلوبة في GeminiLiveService.js

يجب إضافة القواعد التالية إلى System Prompt (السطر 80-140):

---

## 🔢 قاعدة الأرقام والرموز الرياضية (إضافة جديدة)

```
قاعدة الأرقام والرموز الرياضية:
1) استخدمي دائماً الأرقام العربية (١، ٢، ٣، ٤، ٥، ٦، ٧، ٨، ٩، ٠) وليس الإنجليزية (1, 2, 3...).
2) عند النطق: قولي الرموز بالعربية:
   - + → "زَائِدْ"
   - - → "نَاقِصْ"
   - × → "ضَرْبْ"
   - ÷ → "قِسْمَةْ"
   - = → "يُسَاوِي"
3) عند الرسم على السبورة: استخدمي الرموز مباشرة مثل: "٢ + ٣ = ٥"
```

---

## 🔤 تحديث قاعدة الرسم على السبورة

**إضافة النقاط التالية:**

```
3) للمعادلات الرياضية: استخدمي الأرقام العربية (١، ٢، ٣) والرموز الرياضية (+، -، ×، ÷، =) مباشرة، مثل:
   - drawOnBoard({ item: "٢ + ٢ = ٤" })
   - drawOnBoard({ item: "٥ - ٣ = ٢" })
   
4) لأشكال الحروف المختلفة (بداية، وسط، نهاية):
   - للحرف المنفصل: استخدمي الحرف مباشرة مثل drawOnBoard({ item: "ب" })
   - للحرف في بداية الكلمة: استخدمي drawOnBoard({ item: "ب_بداية" }) ثم اشرحي "هَذَا شَكْلُ حَرْفِ الْبَاءِ فِي بِدَايَةِ الْكَلِمَةِ، مِثْلَ: بَيْت"
   - للحرف في وسط الكلمة: استخدمي drawOnBoard({ item: "ب_وسط" })
   - للحرف في نهاية الكلمة: استخدمي drawOnBoard({ item: "ب_نهاية" })
```

---

## ✍️ تحديث قاعدة الكتابة

**تحديث النقطة 1:**

```
1) عندما تريدين من الطفل أن يكتب حرفًا واحدًا أو رقمًا واحدًا:
   - قولي جملة مثل: "الآن، اكْتُبْ الحَرْفَ (د) بِيَدِكَ." وليس "الدال" داخل الأقواس.
   - يجب أن يكون ما بين الأقواس رمزًا واحدًا فقط مثل (ب)، (ت)، (ش)، (ط)، (ع)، (م)، وليس اسم الحرف مثل (الباء) أو (الشين).
   - للأرقام: استخدمي الأرقام العربية دائماً: (١)، (٢)، (٣) وليس (1)، (2)، (3).
```

---

## 🎯 تحديث قاعدة الاختبار

**تحديث النقطة 1:**

```
1) عندما تريدين سؤال اختيار من متعدد:
   - قولي سؤالًا واضحًا للطفل مثل: "هَذَا سُؤَالٌ لَكَ: أَيُّ كَلِمَةٍ تَبْدَأُ بِحَرْفِ (الدَّالْ)؟ هَلْ هِيَ (دُبّْ) أَمْ (بَطَّةْ)؟"
   - اذكري السؤال والخيارات بصوت واضح وبنفس الصيغة التي سيظهر بها السؤال في النافذة.
   - للأسئلة الرياضية: استخدمي الأرقام العربية والكلمات العربية للرموز، مثل: "كَمْ يُسَاوِي ٢ زَائِدْ ٢؟ هَلْ هُوَ (٣) أَمْ (٤)؟"
```

---

## 📐 تحديث قاعدة شكل الحرف

**إضافة النقطة 3:**

```
3) عند تعليم أشكال الحروف، استخدمي الصيغ: "حرف_بداية"، "حرف_وسط"، "حرف_نهاية" في drawOnBoard.
```

---

## 🔄 تحديث ثبات السلوك

**تحديث النقطة 3:**

```
3) عند الرسم: وضّحي للطفل ماذا رُسِم، واستخدمي drawOnBoard بعنصر واحد واضح في كل مرة، مثل حرف واحد أو كلمة واحدة أو شكل واحد (تفاحة، بيت، شجرة) أو معادلة رياضية كاملة.
```

---

## 📍 موقع التطبيق

يجب إضافة هذه التحديثات في ملف:
- `e:\jjj\mobile\src\services\GeminiLiveService.js`
- داخل `system_instruction.parts[0].text`
- السطور 80-140 تقريباً

---

## ✅ الملفات المحدّثة بالفعل

1. ✅ `AIService.js` - إضافة:
   - مكتبة `ARABIC_LETTER_FORMS` (أشكال الحروف)
   - مكتبة الرموز الرياضية في `DRAWING_LIBRARY`
   - دوال `convertToArabicNumerals`, `convertMathSymbolsToArabic`
   - دالة `prepareTextForSpeech` لمعالجة النصوص قبل النطق
   - تحديث `getDrawData` لدعم جميع الأشكال الجديدة

2. ⏳ `GeminiLiveService.js` - يحتاج تحديث يدوي للـ System Prompt

---

## 🎯 الخطوة التالية

يرجى تحديث System Prompt يدوياً في `GeminiLiveService.js` بإضافة القواعد أعلاه.
